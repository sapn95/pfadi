# SwiftPM
.build/
.swiftpm/
Package.resolved

# The .app bundle assembled by scripts/make-app.sh
build/

# Editor / OS
.DS_Store
*.xcuserstate

# Coverage leaves these behind when a binary is run outside scripts/coverage.sh.
*.profraw
